//importing values from another file
//importing values from app.js file and storing in app variable as object
// we need to export values as an object in node js.

//to run this program we need to create a app.js file and with variable a
const app = require("./app.js");

console.log(app.a);
